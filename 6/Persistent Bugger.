def persistence(n):
    count = 0
    while len(str(n)) != 1:
        res = 1
        for x in [int(i) for i in list(str(n))]:
            res *= x
        n = res
        count += 1
    return count
